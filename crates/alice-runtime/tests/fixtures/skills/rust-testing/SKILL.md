---
name: rust-testing
description: Write Rust unit tests and integration tests with assertions.
allowed-tools: "Bash Read Write"
---

# Rust Testing Skill

When writing Rust tests:

1. Use `#[test]` attribute
2. Prefer `assert_eq!` over `assert!`
3. Test edge cases
